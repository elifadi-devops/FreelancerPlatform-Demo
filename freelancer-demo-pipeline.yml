# Minimal University Demo Pipeline
# Guaranteed to work on free tier
trigger: none  # Manual runs only

pool:
  vmImage: 'ubuntu-20.04'  # Most stable free image

jobs:
- job: University_Demo_Validation
  timeoutInMinutes: 2  # Prevents timeout issues
  steps:
  - script: |
      echo "=== UNIVERSITY DEMO PIPELINE ==="
      echo "This proves your pipeline works!"
      echo "Agent location: $(System.DefaultWorkingDirectory)"
      echo "Next we'll add ARM template validation"
    displayName: 'Proof of Concept'
